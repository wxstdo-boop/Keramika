<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme applied to the Android Window while the process is starting when the OS's Dark Mode setting is off -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <!-- Show a splash screen on the activity. Automatically removed when
             the Flutter engine draws its first frame -->
        <item name="android:windowBackground">@drawable/launch_background</item>
        <!-- Непрозрачное окно: иначе MIUI снимает «двойной» снапшот окна
             в недавных приложениях (системный фон + контент). -->
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowDisablePreview">true</item>
        <!-- Edge-to-edge: прозрачный/полупрозрачный статус-бар, чтобы контент
             (со своим скруглением углов) рисовался под ним, а не под чёрной
             работает на MIUI/HyperOS (обычный edge-to-edge они игнорируют). -->
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar">true</item>
    </style>
    <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.

         This Theme is only used starting with V2 of Flutter's Android embedding. -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <!-- Фирменный персиковый фон вместо серого ?android:colorBackground:
             на MIUI/Redmi во время холодного старта (до первого кадра
             Flutter) окно показывало серый «квадрат». Теперь — цвет приложения.
             НО: за клавиатурой при её анимации мелькал белый фон — фон окна
             тёмный (window_background), чтобы никакого «белого зазора» не было. -->
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowDisablePreview">true</item>
        <!-- Edge-to-edge: прозрачный статус-бар, чтобы контент со скруглёнными
             углами рисовался под ним (верхние углы экрана закруглены). -->
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
